# Integration Engineer Agent
FROM agent-dev-base:latest

ENV AGENT_TYPE=integration_engineer
ENV PORT=8004

CMD ["python", "-m", "agent_dev.server", "--agent-type", "integration_engineer", "--port", "8004"]